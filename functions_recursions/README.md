# Q3: Functions & Recursions

Different functions used in the program play different roles. The displayLine() function, for example, is reused to improve readability while the factorial() function is used to demonstrate recursion by repeatedly calling itself until the base case is reached
